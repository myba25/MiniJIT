// Recursive Fibonacci — exercises if/else, comparison, calls, recursion.
fn fib(n) {
  if (n < 2) {
    return n;
  } else {
    return fib(n - 1) + fib(n - 2);
  }
}

fn main() {
  return fib(10);
}
